# Практическое занятие №4 — API + React (Интернет-магазин)

### Бэкенд (порт 3000)
```bash
cd backend
npm install
npm start
```

### Фронтенд (порт 3001)
```bash
cd frontend
npm install

# Установить Sass глобально (один раз)
npm install -g sass

# Запустить с компиляцией SCSS и React одновременно
npm run dev

`http://localhost:3001`